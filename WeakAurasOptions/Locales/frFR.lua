if not WeakAuras.IsCorrectVersion() or not WeakAuras.IsLibsOK() then return end

if GetLocale() ~= "frFR" then
  return
end

local L = WeakAuras.L

--@localization(locale="frFR", format="lua_additive_table", namespace="WeakAuras / Options")@
