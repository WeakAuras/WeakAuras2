if not WeakAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "esMX" then
  return
end

local L = WeakAuras.L

--@localization(locale="esMX", format="lua_additive_table", namespace="WeakAuras / Templates")@
