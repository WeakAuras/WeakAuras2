if not WeakAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "esES" then
  return
end

local L = WeakAuras.L

--@localization(locale="esES", format="lua_additive_table", namespace="WeakAuras / Templates")@
